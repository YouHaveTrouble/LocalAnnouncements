# LocalAnnouncements

Paper plugin inspired by Unstable Universe YouTube series where player login and logout announcements and player kill
messages are only shown to players within a certain radius.

Range of each type of announcement can be configured separately.

Values of -1 fallback to the default behavior of broadcasting to the entire server.
